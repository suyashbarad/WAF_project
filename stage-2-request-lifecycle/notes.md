# Request Lifecycle

## Flow:
Browser → DNS → Server → Application → Response

## DNS
Converts domain name into IP address

## Port
Port is used to identify application on server

Example:
localhost:8000

## TCP
Ensures reliable communication between client and server

## What happens when we type URL?
1. DNS lookup
2. Request sent to server
3. Server processes
4. Response sent back
